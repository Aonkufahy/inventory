{
  "/api/*": {
    "target": "http://localhost:8080",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug",
    "headers": {
      "Authorization": "${req.headers.authorization}"
    },
    "onProxyReq": "function(proxyReq, req, res) { console.log('Proxying request to:', req.url); }",
    "onProxyRes": "function(proxyRes, req, res) { console.log('Proxying response from:', req.url); }"
  }
}